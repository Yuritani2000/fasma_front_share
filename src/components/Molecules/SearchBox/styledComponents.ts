import styled from 'styled-components';
import Color from '../../../styles/Color';
import Space from '../../../styles/Space';

export const Wrapper = styled.div`
  width: 100%;
  background: ${Color.White};
  padding: 9px ${Space.LARGE};
  display: flex;
`;